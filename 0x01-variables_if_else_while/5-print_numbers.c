#include <stdio.h>
/**
* main -entry point
*
* Description :prints all single digit numbers
*
* Return: Always 0 (Success)
*/

int main(void)
{
int num = 0;
while (a <= 9)
{
putchar(num);
num++;
}
putchar('\n');
return (0);
}
